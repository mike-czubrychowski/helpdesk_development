<%= bootstrap_form_for(@ticket_category) do |f| %>
  <% if @ticket_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_category.errors.count, "error") %> prohibited this ticket_category from being saved:</h2>

      <ul>
      <% @ticket_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
    <%= f.text_field :name %>
  <%= f.submit %>
<% end %>
