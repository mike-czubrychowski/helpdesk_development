<%= bootstrap_form_for(@ticket_subcategory) do |f| %>
  <% if @ticket_subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_subcategory.errors.count, "error") %> prohibited this ticket_subcategory from being saved:</h2>

      <ul>
      <% @ticket_subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
    <%= f.collection_select :ticket_category_id, @ticket_categories, :id, :name,  {:include_blank => "Please select...", :class => 'text_field form-control'} %>
  
    <%= f.text_field :name %>
  <%= f.submit %>
<% end %>
