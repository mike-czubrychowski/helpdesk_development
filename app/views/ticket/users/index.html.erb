<div class="row">
	<div class = "col-md-8 col-xs-12">
		<h1>ticket_users</h1>
	</div>
	<div class = "col-md-4 col-xs-12">
		<h2>
			<% if can? :create, @ticket_users %>
		        <%= link_to new_ticket_user_path, :class => 'btn btn-primary', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
		            <i class ="fa fa-plus"></i>
		        <% end -%>
		    <% end %>
		</h2>
	</div>
</div>

<div class="row">
	<table class="table table-striped table-responsive" id ="ticket_users" >
	  <thead>
	    <tr>
		    	<th>Detail</th>
		    	<th>User</th>
		      	<th></th>
	    </tr>
	  </thead>

	  <tbody>
	    <% @ticket_users.each do |ticket_user| %>
	      <tr>
		        <td><%= ticket_user.detail %></td>
		        <td><%= ticket_user.user %></td>
	
	        <td>
	        	<% if can? :read, @ticket_user %>
		            <%= link_to ticket_user_path(@ticket_user), :class => 'btn btn-primary btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
		              <i class ="fa fa-eye"></i>
		            <% end -%>
		        <% end %>
		        <% if can? :edit, @ticket_user %>
		            <%= link_to edit_ticket_user_path(@ticket_user), :class => 'btn btn-primary btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
		              <i class ="fa fa-edit"></i>
		            <% end -%>
		        <% end %>
		        <% if can? :destroy, @ticket_user %>
				    <%= link_to ticket_user_path(@ticket_user), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-primary btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
				      <i class ="fa fa-times"></i>
				    <% end -%>
				<% end %>
	        

	        </td>
	        
	      </tr>
	    <% end %>
	  </tbody>
	</table>
</div>
<br>
<br>

<%= link_to 'New User', new_ticket_user_path %>
