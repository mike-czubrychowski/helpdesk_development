<%= bootstrap_form_for(@ticket_statistic) do |f| %>
  <% if @ticket_statistic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_statistic.errors.count, "error") %> prohibited this ticket_statistic from being saved:</h2>

      <ul>
      <% @ticket_statistic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
    <%= f.collection_select :location_id, @locations, :id, :name,  {:include_blank => "Please select...", :class => 'text_field form-control'} %>
  <%= f.submit %>
<% end %>
