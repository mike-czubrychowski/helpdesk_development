<%= bootstrap_form_for(@ticket_sla) do |f| %>
  <% if @ticket_sla.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_sla.errors.count, "error") %> prohibited this ticket_sla from being saved:</h2>

      <ul>
      <% @ticket_sla.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
    <%= f.text_field :name %>
  
    <%= f.number_field :order %>
  
    <%= f.collection_select :ticket_category_id, @ticket_categories, :id, :name,  {:include_blank => "Please select...", :class => 'text_field form-control'} %>
  
    <%= f.text_field :ticket_priority %>
  
    <%= f.text_field :time %>
  <%= f.submit %>
<% end %>
